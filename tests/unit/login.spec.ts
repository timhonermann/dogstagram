import { FirebaseMock } from "@/testing/firebase.mock";
import firebase from "firebase/app";

describe("Login", () => {
  test("should call firebase", () => {
    // Arrange
    // Act
    // Assert
  });
});
